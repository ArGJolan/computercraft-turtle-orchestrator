const colors = require('./colors')
const timestamp = require('./timestamp')
const logger = require('./logger')
const array = require('./array')
const { usleep, sleep } = require('./sleep')

module.exports = {
  colors,
  timestamp,
  logger,
  sleep,
  usleep,
  array
}
